Your snippet creates **VPC → Subnet → EKS**, but it is incomplete. A **working Terraform EKS infrastructure** requires multiple services:

* VPC
* Subnets (public/private)
* Internet Gateway
* Route Tables
* Security Groups
* IAM Roles
* EKS Cluster
* EKS Node Group
* Outputs

Below is a **complete Terraform project structure** used in real environments.

---

# 1️⃣ Terraform Folder Structure

```
terraform-eks/
│
├── provider.tf
├── variables.tf
├── vpc.tf
├── iam.tf
├── eks-cluster.tf
├── node-group.tf
├── outputs.tf
└── terraform.tfvars
```

---

# 2️⃣ provider.tf

```hcl
provider "aws" {
  region = var.region
}

terraform {
  required_version = ">= 1.3"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}
```

---

# 3️⃣ variables.tf

```hcl
variable "region" {
  default = "us-east-1"
}

variable "cluster_name" {
  default = "dev-cluster"
}

variable "vpc_cidr" {
  default = "10.0.0.0/16"
}
```

---

# 4️⃣ vpc.tf

```hcl
resource "aws_vpc" "dev_vpc" {
  cidr_block = var.vpc_cidr

  tags = {
    Name = "dev-vpc"
  }
}

resource "aws_internet_gateway" "gw" {
  vpc_id = aws_vpc.dev_vpc.id
}

resource "aws_subnet" "public_subnet" {
  vpc_id                  = aws_vpc.dev_vpc.id
  cidr_block              = "10.0.1.0/24"
  availability_zone       = "us-east-1a"
  map_public_ip_on_launch = true
}

resource "aws_route_table" "public_rt" {
  vpc_id = aws_vpc.dev_vpc.id
}

resource "aws_route" "internet_access" {
  route_table_id         = aws_route_table.public_rt.id
  destination_cidr_block = "0.0.0.0/0"
  gateway_id             = aws_internet_gateway.gw.id
}

resource "aws_route_table_association" "a" {
  subnet_id      = aws_subnet.public_subnet.id
  route_table_id = aws_route_table.public_rt.id
}
```

---

# 5️⃣ iam.tf

```hcl
resource "aws_iam_role" "eks_role" {
  name = "eks-cluster-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect = "Allow"
      Principal = {
        Service = "eks.amazonaws.com"
      }
      Action = "sts:AssumeRole"
    }]
  })
}

resource "aws_iam_role_policy_attachment" "eks_policy" {
  role       = aws_iam_role.eks_role.name
  policy_arn = "arn:aws:iam::aws:policy/AmazonEKSClusterPolicy"
}
```

---

# 6️⃣ eks-cluster.tf

```hcl
resource "aws_eks_cluster" "dev_cluster" {
  name     = var.cluster_name
  role_arn = aws_iam_role.eks_role.arn

  vpc_config {
    subnet_ids = [
      aws_subnet.public_subnet.id
    ]
  }

  depends_on = [
    aws_iam_role_policy_attachment.eks_policy
  ]
}
```

---

# 7️⃣ node-group.tf

```hcl
resource "aws_iam_role" "node_role" {
  name = "eks-node-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect = "Allow"
      Principal = {
        Service = "ec2.amazonaws.com"
      }
      Action = "sts:AssumeRole"
    }]
  })
}

resource "aws_iam_role_policy_attachment" "worker_node_policy" {
  role       = aws_iam_role.node_role.name
  policy_arn = "arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy"
}

resource "aws_eks_node_group" "nodes" {
  cluster_name    = aws_eks_cluster.dev_cluster.name
  node_group_name = "dev-nodes"
  node_role_arn   = aws_iam_role.node_role.arn

  subnet_ids = [
    aws_subnet.public_subnet.id
  ]

  scaling_config {
    desired_size = 2
    max_size     = 3
    min_size     = 1
  }

  instance_types = ["t3.medium"]
}
```

---

# 8️⃣ outputs.tf

```hcl
output "cluster_name" {
  value = aws_eks_cluster.dev_cluster.name
}

output "cluster_endpoint" {
  value = aws_eks_cluster.dev_cluster.endpoint
}
```

---

# 9️⃣ terraform.tfvars

```hcl
region       = "us-east-1"
cluster_name = "dev-cluster"
vpc_cidr     = "10.0.0.0/16"
```

---

# 🔟 Commands to Deploy

```bash
terraform init
terraform plan
terraform apply
```

---

# ✅ Architecture Created

```
AWS
 ├── VPC
 │    ├── Subnet
 │    ├── Internet Gateway
 │    └── Route Table
 │
 ├── IAM
 │    ├── EKS Role
 │    └── Node Role
 │
 └── EKS
      ├── Cluster
      └── Node Group
```

---

💡 If you want, I can also show you a **production-level Terraform EKS architecture used by companies (20+ files, modules, ALB, autoscaling, private subnets)** which is what DevOps engineers actually use in interviews.
